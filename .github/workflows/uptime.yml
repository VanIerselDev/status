name: uptime (disabled - migrated to UptimeRobot)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This monitor was migrated to UptimeRobot. See https://stats.uptimerobot.com/zR5WYcKr6N"
